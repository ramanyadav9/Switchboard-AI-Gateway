use_default_settings: true

server:
  secret_key: "switchboard-searxng-secret-change-me"
  bind_address: "0.0.0.0"
  port: 8888

search:
  formats:
    - html
    - json
  default_lang: "en"
  safe_search: 2
